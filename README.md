# GIT-TASKS
Git task Repo 

CREATE a git repo with three different branches and push the code from your local and access in remote server with with particular yml file and trigger the enviroment (Using Git actions).
